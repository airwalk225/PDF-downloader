# PDF-downloader
Docker container to download any non downloaded anything from a site  
Just open up the `downloadPDF.php` adjust the folder keys to be the sub folders you want to download the the given links resources to.  
Adjust the link to the URL you want to scrape  
The change the filter to be a DOMCrawler filter or Xpath  
Run with `startPDFDownload.sh`
